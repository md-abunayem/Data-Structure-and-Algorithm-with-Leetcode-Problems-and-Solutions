#include<iostream>
using namespace std;

int main(){
    int num = 10;
    int shift = 2;

    cout << "Bitwise right shift:" << (num >> shift);
}

/*
1.first - convert numbers to binary 
2. apply right shift operation based on provided shift number or steps
*/